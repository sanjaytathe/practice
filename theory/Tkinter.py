'''
import tkinter import *
root=Tk() :- first line
mainloop :- for print code / last line
resizalble(0,0) :- to give  fixed size
geometry :- to give size
title :- to give title
Label :- to edit in frame
text,bg,fg,width,height,bd,font,textvariablle,bitmap,
pack()
grid(row,column)
place(x,y)
maxsize(width=600,height=300)
minsize(width=600,height=300)

Entry :- use for making button

textvariable :- var
var=StringVar()

iconbitmap :0 first of all convert image png to ico
the right click on image copy as path and
paste into code'''